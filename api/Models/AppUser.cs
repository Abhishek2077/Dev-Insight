using Microsoft.AspNetCore.Identity;

namespace api.Models;

public class AppUser : IdentityUser
{
    // We can add custom properties for our users here later!
    // For example: public string? DisplayName { get; set; }
}